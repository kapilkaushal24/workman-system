services:

  workman.auth:
    container_name: workman-auth
    build:
      context: .
      dockerfile: src/WorkMan/WORKMAN.Auth/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__AuthDatabase: Host=authdb;Port=5432;Database=AuthDb;Username=postgres;Password=postgres
      RabbitMQ__Host: rabbitmq
      RabbitMQ__Port: 5672
      RabbitMQ__Username: guest
      RabbitMQ__Password: guest
    ports:
      - "5001:8080"
    depends_on:
      authdb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - workman-network

  workman.config:
    container_name: workman-config
    build:
      context: .
      dockerfile: src/WorkMan/WORKMAN.Config/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__configDatabase: Host=configdb;Port=5432;Database=ConfigDB;Username=postgres;Password=postgres
      RabbitMQ__Host: rabbitmq
      RabbitMQ__Port: 5672
      RabbitMQ__Username: guest
      RabbitMQ__Password: guest
    ports:
      - "5002:8080"
    depends_on:
      configdb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    networks:
      - workman-network